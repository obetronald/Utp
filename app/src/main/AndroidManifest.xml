<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.guiautpbea">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".Laboratorios"></activity>
        <activity android:name=".Aulas" />
        <activity android:name=".Universidad" />
        <activity
            android:name=".Modulo3"
            android:label="@string/title_activity_modulo3"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Bienvenido" />
        <activity android:name=".Registro" />
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>